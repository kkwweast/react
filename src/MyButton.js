import { useState} from "react";
function MyButton(){
const [count, setCount] = useState(0);
const [, setGreeting] = useState9("Hello World");
      
      
}

console.log(prop,foods);

    function Mybutton () {
        console.log('Handling the click  event')
        "something".toLowerCase();
        setCount(count + 1);
        if (greeting == greeting.toLocalUpperCase()) {
            setGreeting(greeting.toLowerCase());
          }else{  
        setgreeting(greeting.toUpperCase());
        
        
    }
props.foods[]
return (
    
    <div>
        <p>{'The current food is: ${props,foods[count % props.foods.length]}'}</p>
        {count}</button > 
         <button onClick={handClick}> 
        <button onClick={handleClick}>I'm a button {count}
         {count}</button>
    <p>{greeting}</p>
    </div>
);




export default MyButton;
function displayOnClick(numbers){
    const [count] = useState(0);
    return(
        <div>
            <p>{count}</p>
            <h1>Click the Button</h1>
        </div>
    )
}









 //How we used to do  in the previous
 }